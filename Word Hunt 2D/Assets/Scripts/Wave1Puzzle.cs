using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Wave1Puzzle : PuzzleGameManager
{
    //public GameObject wave1GameObject;
    // Start is called before the first frame update
    new void Start()
    {
        base.Start();
        //wave1GameObject = GetComponent<GameObject>();
    }

    // Update is called once per frame
    void Update()
    {
        //if (isPuzzleComplete == true)
        //{
        //    wave1GameObject.SetActive(false);
        //}
    }
}
